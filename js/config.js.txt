/* ─────────────────────────────────────
   글핏 · config.js (로컬 설정 및 API 키)
   ───────────────────────────────────── */

const CONFIG = {
  // 본인의 실제 OpenAI API 키를 아래에 입력하세요.
  // 주의: 이 파일은 .gitignore에 등록되어 Git 추적에서 제외됩니다.
  OPENAI_API_KEY: 'sk-proj-여기에_실제_API_키를_입력하세요'
};